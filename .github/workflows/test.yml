name: test

on:
  pull_request:
  push:
    branches: [master]

jobs:
  validate-id:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install hypher engine (pinned commit)
        run: npm install "git+https://github.com/mlengse/hypher.git#a336187de24260b11749d1caf6c6903d4f5d9a62" --no-save
      - name: Validate Indonesian patterns vs KBBI
        run: node test/validate-id.js
